package com.spoj.basic;

import java.util.HashMap;
import java.util.Map;

/**
 * Created by ahbagwan on 6/29/2016.
 */
public class SudokuValid {
    private static final board[][] = {}

    private Map<Integer, Integer> columnMap = new HashMap<>();
    private Map<Integer, Integer> rowMap = new HashMap<>();
    private Map<Integer, Integer> gridMap = new HashMap<>();

    public boolean isValidSudoku(char[][] board) {
        // each horizontal row contains a digit once
        // each vertical column contains a digit once
        // each grid contains a digit once

    }
}
